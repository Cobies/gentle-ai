# Cross-runtime model picker parity

Objective: Complete the supported model configuration surfaces across OpenCode, Claude Code and Codex, using Pi/Gentle Shell as a reference without changing Pi or unsupported runtimes.

Problem: OpenCode's managed native `general` and `explore` agents cannot be edited in the picker; Claude's new RDD rows need short-terminal usability proof; Codex's custom picker omits distinct ODD and six RDD roles, while its isolated RDD CLI adapter ignores saved role models.

Authorized scope: Complete these gaps, preserve existing presets, fallback behavior, native review isolation and SDD support. Do not alter unrelated files or other runtimes. User requested one PR for all work with documented `size:exception`; push, PR and conditional merge to Gentleman-Programming/gentle-ai using the current gh/Git SSH session explicitly authorized. Issue #4911 was approved retrospectively with target-host-verified maintainer authority; PR #4912 has verified `type:feature` and `size:exception` labels. Merge remains conditional on passing required checks and human contribution responsibility. No remote work outside that destination/operation.

TDD: ODD mode unconfirmed (OpenSpec strict TDD is not ODD authority); run ordinary focused Go tests and functional harness checks. Delivery: single-pr, size exception explicitly requested; forecast >400 authored changed lines due to three runtime surfaces, tests, and review evidence. Not a license to omit tests or compress code. Branch feat/claude-native-rdd-models, base 21032461; prior Claude units aa96bec9 and 70bcc253 were native-approved and acknowledged in review-c6a1cb284be40c22. Engram mirror `odd/cross-runtime-model-picker-parity/tasks` pending if callable session-bound memory is unavailable.

- [x] P1: Expose OpenCode native general/explore as individually editable global picker rows. Preserve SDD/custom bulk actions, profile behavior and installed managed definitions. Tests: picker navigation, persistence/config read, overlay injection when absent/present. Route: delegated writer, multi-file preparation/write. Evidence: focused picker/injection tests passed; `env -u PI_CODING_AGENT_DIR go test ./internal/tui/screens ./internal/components/sdd -count=1` passed; `git diff --check` passed. Runtime boundary: temporary-config injection test (live OpenCode session N/A). Commit: `90e7ac5d`. Run with `PI_CODING_AGENT_DIR` unset to avoid the external-file incident.
- [x] P2: Make Claude custom RDD rows usable in short terminals; verify picker navigation and selection without changing named presets. Tests: cursor-following viewport at 12 lines covering six RDD rows and Confirm; focused Claude picker/TUI tests passed with `PI_CODING_AGENT_DIR` unset. Route: delegated writer, three-file edit. Commit: `dc72bd1e`.
- [x] P3: Add Codex Custom ODD Explorer/Worker/Verify and six RDD reviewer choices with persistence and safe defaults. User selected preset ODD mapping Explorer→cheap, Worker→mid, Verify→strong. A fresh authorized `codex debug models` query confirmed published `gpt-6-astra`, `gpt-6-sol`, `gpt-6-luna`; older local cache/bundled catalog was stale. Updated product policy: replace all old Sol with Sol 6 and Terra/Luna with Luna 6; Powerful alone uses Astra 6 for strong carril and orchestrator. No 5.6 default in any preset. Model costs were asserted by user, not independently measured. Preserve SDD and existing isolation; RDD choices stay Custom with native default when unset. Tests: picker/round-trip/persistence/table rendering and short-terminal Confirm; `env -u PI_CODING_AGENT_DIR go test ./internal/model ./internal/tui/screens ./internal/tui ./internal/components/sdd ./internal/app -count=1` passed after stale expectations were updated. Route: delegated mapper then writer. Commit: `19b8f72d`.
- [x] P4: Wire valid saved Codex RDD role models into actual isolated `codex exec --model` for four lenses, refuter and validator; preserve `--ignore-user-config`, sandbox and fallbacks. Tests: six `rdd-*` keys, short-key rejection, invalid/missing assignments, command arguments and isolation; independent focused verifier passed. Real Codex generation was not run; helper subprocess exercises command construction. Route: delegated writer. Commit: `49242983`.
- [ ] P5: Run focused and relevant broad checks and independent assessment/review; preserve known local side-effect limitations. Seven focused packages, gofmtcheck and diff check passed after merging main. CI Unit Tests on PR #4912 exposed seven stale GPT-5.6 Codex preset/default assertions and three goldens; six-file fixture correction independently passed four affected packages, golden tests, gofmtcheck and diff check; commit and CI rerun pending. Full `go test ./...` withheld locally because an MCP integration test invokes installed `claude mcp list` with unverified external-state effects. RDD approved and acknowledged both pre-merge `review-d5c18225785e966d` and merged feature range `review-06630e39ba9a4fa3`; the next fixture-only commit must be assessed separately. Route: delegated verifier and parent native review.
- [ ] P6: PR #4912 is open against main; issue #4911 has `status:approved`, and PR labels `type:feature` and `size:exception` were applied under verified `ADMIN` authority with read-back. The 1248-line rationale and incomplete checks are disclosed in the PR body. Merge only after CI and responsibility gates pass. Route: parent remote operations after checks.

Progress: P1–P4 committed. Formatting unit `109c6a7c`, merge-main resolution `7469016c`, and merged-tree RDD completed. PR #4912 is open with policy labels; CI Unit Tests failed on stale preset fixtures, now corrected locally but not committed. Next: commit fixture corrections, assess RDD for the new candidate, push, inspect CI and merge only if gates permit. Incident: inherited `PI_CODING_AGENT_DIR` caused a pre-existing safeguard test to write `~/.gentle-shell/agent/APPEND_SYSTEM.md`; prior contents unknown, no blind restoration. Same failure reproduced on prior HEAD; focused checks with override unset left external-file metadata unchanged. Preserve unrelated pre-existing untracked files.
