# Spend-Proportionate ODD Routing (issue #4894)

## Objective
Align the canonical ODD routing contract with the removed overbroad gentle-pi runtime gate. Delegation remains mandatory for two or more non-trivial files; one small mechanical second-file edit is not rejected solely because a first file was touched. Make planning and verification proportional to operator spend without a new counter, gate, or unsafe line-count exemption.

## Scope and constraints
- Canonical source: `internal/components/agentguidance/routing.go`; update its direct routing tests and any actual consumer contract snapshots that fail because wording changed.
- Shipped gentle-pi `assets/orchestrator-delegation.md` already carries premise-first, sibling reuse and focused-then-full checks in work-unit commit `2c48ce124f5055e7859a864281f2035d177d039a` on its isolated feature branch. The matching package task record is `odd/tasks/odd-proportional-delegation.md` there.
- Preserve mandatory delegation for two or more non-trivial files, TDD requirements, full applicable verification at closure, native RDD ownership, and all consent/safety gates.
- Keep edits inside this isolated worktree. The main gentle-ai worktree has unrelated tracked changes; never reset, alter, or stage them. No remote delivery is authorized.

## Tasks
- [x] OSP-1 (delegated writer) — Add canonical spend-proportional guidance: validate consequential premises before building, reuse sibling investigation evidence, use focused tests while iterating and applicable full checks at closure. Make clear a mechanical second-file edit does not trigger the two-non-trivial-file rule. Update bounded routing contract tests.
- [ ] OSP-2 — Verify Go routing tests and generated consumer text against shipped gentle-pi guidance, record evidence and a work-unit commit on this branch. Assess native RDD for that committed candidate if enabled. Implementation and focused checks are complete; full-suite closure is blocked below.
- [ ] OSP-3 (check blocker) — Obtain a completed applicable full-suite result in an environment where offline child builds and local CodeGraph fixture isolation can run; the 240s attempt has no suite-wide outcome. Do not classify its timeout or unrelated tests without new evidence.

## Checks and progress
TDD mode: unresolved from existing project/session configuration; do not claim RED without observed evidence. Focused runner candidate: `go test ./internal/components/agentguidance`. Also run the relevant contract/injection suite identified by the writer. Engram mirror pending: memory tools unavailable in the current parent session.

2026-09-22: Isolated worktree created at this branch from local `main` (`af4ce122`). Cross-clone `subagent_run` from the gentle-pi parent is rejected because it only accepts worktrees of the parent's Git clone. No source edits were attempted there.

OSP-1: A bounded writer in this clone reviewed the existing uncommitted diff and found no additional edits necessary. `GOPROXY=off GOSUMDB=off go test ./internal/components/agentguidance ./internal/components/sdd ./internal/agents/capabilitymanifest` passed (all three packages, cached). The mandatory writer threshold remains two non-trivial files; a second mechanical edit alone does not satisfy it. TDD mode remains unresolved, and no RED evidence is claimed.

OSP-2 checks so far: `GOPROXY=off GOSUMDB=off go test -count=1 ./internal/components/agentguidance` passed (1.202s); `GOPROXY=off GOSUMDB=off go test -count=1 -run '^TestInjectRoutingInstallsGuidanceForEverySupportedAgent$' ./internal/components/agentguidance` passed (0.125s). Rendered routing and injection assertions cover the contract; no tested consumer snapshot failed. The local gentle-pi commit `2c48ce124f5055e7859a864281f2035d177d039a:assets/orchestrator-delegation.md` confirms premise-first checks, sibling reuse, and focused checks followed by the applicable full suite; it retains the mandatory two-non-trivial-file threshold. This is a semantic comparison, not a byte-for-byte snapshot. An independent read-only investigation found the local module cache contains all 37 `go.mod` requirements and 57 full-module `go.sum` entries, but ran no failing test or baseline; the earlier interrupted full-suite error and catalog failure are not attributable or preexisting on that evidence. Offline targeted catalog: `GOPROXY=off GOSUMDB=off go test -count=1 ./internal/assets -run '^TestOpenCodeV2CatalogAndRegistry$' -v` passed (exit 0; test 0.21s). A pristine-HEAD catalog comparison was unnecessary because this candidate test passed. `GOPROXY=off GOSUMDB=off go test ./...` was interrupted at a 240s timeout with no suite-wide result. Before timeout, `internal/agents/pi` reported two CodeGraph child-discovery failures (`adapter_test.go:142,191`) and `internal/app` reported two offline child-build failures (`app_test.go:705`, `tty_boundary_test.go:241`) because module lookup was disabled. The four focused failures also reproduce on a disposable clean `HEAD` (`af4ce122b0236db30bff5c0320587482c4b15a55`) snapshot and the candidate using the same offline commands: `go test -count=1 ./internal/agents/pi -run '^(TestDiscoverCodeGraphChildrenUsesProjectOverrideAndPreservesPackageSource|TestDiscoverCodeGraphChildrenUsesNormalizedRuntimeIdentity)$'` and `go test -count=1 ./internal/app -run '^(TestTUIExecuteWithBackgroundPreservesConcurrentCLIStateMutation|TestBuiltBinaryClosedStdinRefusesBeforeBubbleTea)$'` (both with `GOPROXY=off GOSUMDB=off`). The Pi tests include local Gentle Shell children beyond the expected fixture; the app tests stop at an offline module-lookup failure before exercising their intended behavior. These four failures are baseline-matched, not introduced by this candidate. The full-suite timeout itself has no established cause. No dependencies were downloaded or remote access used. Runtime harness: N/A because this change renders guidance rather than introducing a runtime service boundary; injection behavior is tested by `TestInjectRoutingInstallsGuidanceForEverySupportedAgent`. Rollback boundary: revert this work-unit commit to remove only the routing guidance, its direct assertions, and this task record. Remaining check: a completed full offline suite is unavailable under the observed 240s timeout and offline child-build limitations. Engram mirror remains pending because memory tools are unavailable.

Work-unit commit: `23f4a3ef` (`fix(agents): make ODD guidance spend-proportionate`), containing the routing contract, its assertions, and the task record. Native RDD is on (global). `gentle-ai review assess --cwd <this worktree> --agent pi --base-ref af4ce122b0236db30bff5c0320587482c4b15a55 --committed-only --json` returned medium risk (`executable_change` in `routing.go`), 3 paths, 34 changed lines, `review_due: false`, `review_due_reason: under_budget`. No native review started; this work unit remains in the medium slice until the delivery budget is reached. Pi's `gentle_review` assessment wrapper separately failed with `schema-incompatible` and yielded no native risk; the successful native CLI output above is the recorded assessment, not an inferred low tier.
